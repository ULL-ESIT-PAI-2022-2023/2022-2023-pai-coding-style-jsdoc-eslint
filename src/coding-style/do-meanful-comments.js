let value = 5; // set value to 5
let string = 'Some string'; // set string to 'Some string'

for (char for string) { // iterates through the string
  statement;
}

// increase the value of count
++count;

// add two numbers
function sumTwoNumbers(addend1, addend2) {
  return addend1 + addend2; // sum of two numbers
}
